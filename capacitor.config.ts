import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.esteban.accenture.todoapp',
  appName: 'Accenture Todo App',
  webDir: 'www'
};

export default config;
